# Gemini API Key (Required for AI note generation and fallback audio transcription)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Preferred language for notes (default: en)
NOTES_LANGUAGE=en

# Optional: Default directory structure format (date or subject)
ORGANIZATION_MODE=date
